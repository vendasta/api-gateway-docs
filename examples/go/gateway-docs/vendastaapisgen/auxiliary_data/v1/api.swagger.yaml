swagger: "2.0"
info:
  title: auxiliary_data/v1/api.proto
  version: version not set
tags:
  - name: AuxiliaryFieldData
  - name: AuxiliaryFieldSchema
consumes:
  - application/json
produces:
  - application/json
paths:
  /auxiliarydata.v1.AuxiliaryFieldData/ListAuxiliaryData:
    post:
      operationId: AuxiliaryFieldData_ListAuxiliaryData
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/v1ListAuxiliaryDataResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/v1ListAuxiliaryDataRequest'
      tags:
        - AuxiliaryFieldData
  /auxiliarydata.v1.AuxiliaryFieldData/UpsertAuxiliaryData:
    post:
      operationId: AuxiliaryFieldData_UpsertAuxiliaryData
      responses:
        "200":
          description: A successful response.
          schema:
            type: object
            properties: {}
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/v1UpsertAuxiliaryDataRequest'
      tags:
        - AuxiliaryFieldData
  /auxiliarydata.v1.AuxiliaryFieldSchema/ArchiveAuxiliaryDataFieldSchema:
    post:
      operationId: AuxiliaryFieldSchema_ArchiveAuxiliaryDataFieldSchema
      responses:
        "200":
          description: A successful response.
          schema:
            type: object
            properties: {}
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/v1ArchiveAuxiliaryDataFieldSchemaRequest'
      tags:
        - AuxiliaryFieldSchema
  /auxiliarydata.v1.AuxiliaryFieldSchema/CreateAuxiliaryDataFieldSchema:
    post:
      operationId: AuxiliaryFieldSchema_CreateAuxiliaryDataFieldSchema
      responses:
        "200":
          description: A successful response.
          schema:
            type: object
            properties: {}
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/v1CreateAuxiliaryDataFieldSchemaRequest'
      tags:
        - AuxiliaryFieldSchema
  /auxiliarydata.v1.AuxiliaryFieldSchema/GetAuxiliaryDataFieldSchema:
    post:
      operationId: AuxiliaryFieldSchema_GetAuxiliaryDataFieldSchema
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/v1GetAuxiliaryDataFieldSchemaResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/v1GetAuxiliaryDataFieldSchemaRequest'
      tags:
        - AuxiliaryFieldSchema
  /auxiliarydata.v1.AuxiliaryFieldSchema/GetMultiAuxiliaryDataFieldSchema:
    post:
      operationId: AuxiliaryFieldSchema_GetMultiAuxiliaryDataFieldSchema
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/v1GetMultiAuxiliaryDataFieldSchemaResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/v1GetMultiAuxiliaryDataFieldSchemaRequest'
      tags:
        - AuxiliaryFieldSchema
  /auxiliarydata.v1.AuxiliaryFieldSchema/ListAuxiliaryDataFieldSchema:
    post:
      operationId: AuxiliaryFieldSchema_ListAuxiliaryDataFieldSchema
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/v1ListAuxiliaryDataFieldSchemaResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/v1ListAuxiliaryDataFieldSchemaRequest'
      tags:
        - AuxiliaryFieldSchema
  /auxiliarydata.v1.AuxiliaryFieldSchema/UnarchiveAuxiliaryDataFieldSchema:
    post:
      operationId: AuxiliaryFieldSchema_UnarchiveAuxiliaryDataFieldSchema
      responses:
        "200":
          description: A successful response.
          schema:
            type: object
            properties: {}
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/v1UnarchiveAuxiliaryDataFieldSchemaRequest'
      tags:
        - AuxiliaryFieldSchema
  /auxiliarydata.v1.AuxiliaryFieldSchema/UpdateAuxiliaryDataFieldSchema:
    post:
      operationId: AuxiliaryFieldSchema_UpdateAuxiliaryDataFieldSchema
      responses:
        "200":
          description: A successful response.
          schema:
            type: object
            properties: {}
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/v1UpdateAuxiliaryDataFieldSchemaRequest'
      tags:
        - AuxiliaryFieldSchema
definitions:
  ListAuxiliaryDataFieldSchemaRequestFilters:
    type: object
    properties:
      onlyArchived:
        type: boolean
  protobufAny:
    type: object
    properties:
      '@type':
        type: string
    additionalProperties: {}
  rpcStatus:
    type: object
    properties:
      code:
        type: integer
        format: int32
      message:
        type: string
      details:
        type: array
        items:
          type: object
          $ref: '#/definitions/protobufAny'
  v1ArchiveAuxiliaryDataFieldSchemaRequest:
    type: object
    properties:
      identifier:
        $ref: '#/definitions/v1FieldSchemaIdentifier'
  v1AuxiliaryDataFieldSchema:
    type: object
    properties:
      partnerId:
        type: string
      fieldId:
        type: string
      fieldType:
        $ref: '#/definitions/v1AuxiliaryDataFieldType'
      fieldName:
        type: string
      fieldDescription:
        type: string
      created:
        type: string
        format: date-time
      updated:
        type: string
        format: date-time
      dropdownOptions:
        type: array
        items:
          type: object
          $ref: '#/definitions/v1DropdownOption'
      currencyCode:
        type: string
        title: currency code according to ISO-4217, e.g. 'USD', 'CAD', 'BRL', etc
      archived:
        type: string
        format: date-time
      externalId:
        type: string
  v1AuxiliaryDataFieldType:
    type: string
    enum:
      - AUXILIARY_DATA_FIELD_TYPE_INVALID
      - AUXILIARY_DATA_FIELD_TYPE_STRING
      - AUXILIARY_DATA_FIELD_TYPE_INTEGER
      - AUXILIARY_DATA_FIELD_TYPE_DATE
      - AUXILIARY_DATA_FIELD_TYPE_DROPDOWN
      - AUXILIARY_DATA_FIELD_TYPE_CURRENCY
    default: AUXILIARY_DATA_FIELD_TYPE_INVALID
  v1AuxiliaryDataObjectID:
    type: object
    properties:
      partnerId:
        type: string
      objectId:
        type: string
  v1CreateAuxiliaryDataFieldSchemaRequest:
    type: object
    properties:
      fieldSchema:
        $ref: '#/definitions/v1AuxiliaryDataFieldSchema'
  v1DropdownOption:
    type: object
    properties:
      label:
        type: string
      value:
        type: string
  v1FieldDataUpsertOperation:
    type: object
    properties:
      fieldDataId:
        type: string
      externalId:
        type: string
      integer:
        type: string
        format: int64
      string:
        type: string
      date:
        type: string
        format: date-time
      dropdownValue:
        type: string
      currencyValue:
        type: string
        format: int64
      deleteValue:
        type: boolean
        title: deletes the field's current value from the object
  v1FieldSchemaIdentifier:
    type: object
    properties:
      fieldId:
        type: string
      externalId:
        type: string
      partnerId:
        type: string
  v1GetAuxiliaryDataFieldSchemaRequest:
    type: object
    properties:
      identifier:
        $ref: '#/definitions/v1FieldSchemaIdentifier'
  v1GetAuxiliaryDataFieldSchemaResponse:
    type: object
    properties:
      fieldSchema:
        $ref: '#/definitions/v1AuxiliaryDataFieldSchema'
  v1GetMultiAuxiliaryDataFieldSchemaRequest:
    type: object
    properties:
      identifiers:
        type: array
        items:
          type: object
          $ref: '#/definitions/v1FieldSchemaIdentifier'
  v1GetMultiAuxiliaryDataFieldSchemaResponse:
    type: object
    properties:
      fieldSchemas:
        type: array
        items:
          type: object
          $ref: '#/definitions/v1AuxiliaryDataFieldSchema'
  v1ListAuxiliaryDataFieldSchemaRequest:
    type: object
    properties:
      partnerId:
        type: string
      pagingOptions:
        $ref: '#/definitions/vendastatypesPagedRequestOptions'
      filters:
        $ref: '#/definitions/ListAuxiliaryDataFieldSchemaRequestFilters'
  v1ListAuxiliaryDataFieldSchemaResponse:
    type: object
    properties:
      fieldSchemas:
        type: array
        items:
          type: object
          $ref: '#/definitions/v1AuxiliaryDataFieldSchema'
      pagingMetadata:
        $ref: '#/definitions/vendastatypesPagedResponseMetadata'
  v1ListAuxiliaryDataRequest:
    type: object
    properties:
      auxiliaryDataObjectId:
        $ref: '#/definitions/v1AuxiliaryDataObjectID'
      pagingOptions:
        $ref: '#/definitions/vendastatypesPagedRequestOptions'
  v1ListAuxiliaryDataResponse:
    type: object
    properties:
      jsonSchema:
        type: string
      jsonData:
        type: string
      pagingMetadata:
        $ref: '#/definitions/vendastatypesPagedResponseMetadata'
  v1UnarchiveAuxiliaryDataFieldSchemaRequest:
    type: object
    properties:
      identifier:
        $ref: '#/definitions/v1FieldSchemaIdentifier'
  v1UpdateAuxiliaryDataFieldSchemaRequest:
    type: object
    properties:
      fieldSchema:
        $ref: '#/definitions/v1AuxiliaryDataFieldSchema'
      fieldMask:
        $ref: '#/definitions/vendastatypesFieldMask'
        description: Supported updates for name, and description only at this time.
  v1UpsertAuxiliaryDataRequest:
    type: object
    properties:
      auxiliaryDataObjectId:
        $ref: '#/definitions/v1AuxiliaryDataObjectID'
      fieldDataUpsertOperations:
        type: array
        items:
          type: object
          $ref: '#/definitions/v1FieldDataUpsertOperation'
  vendastatypesFieldMask:
    type: object
    properties:
      paths:
        type: array
        items:
          type: string
        description: The set of field mask paths.
    description: |-
      paths: "f.a"
          paths: "f.b.d"

      Here `f` represents a field in some root message, `a` and `b`
      fields in the message found in `f`, and `d` a field found in the
      message in `f.b`.

      Field masks are used to specify a subset of fields that should be
      returned by a get operation or modified by an update operation.
      Field masks also have a custom JSON encoding (see below).

      # Field Masks in Projections

      When used in the context of a projection, a response message or
      sub-message is filtered by the API to only contain those fields as
      specified in the mask. For example, if the mask in the previous
      example is applied to a response message as follows:

          f {
            a : 22
            b {
              d : 1
              x : 2
            }
            y : 13
          }
          z: 8

      The result will not contain specific values for fields x,y and z
      (their value will be set to the default, and omitted in proto text
      output):


          f {
            a : 22
            b {
              d : 1
            }
          }

      A repeated field is not allowed except at the last position of a
      paths string.

      If a FieldMask object is not present in a get operation, the
      operation applies to all fields (as if a FieldMask of all fields
      had been specified).

      Note that a field mask does not necessarily apply to the
      top-level response message. In case of a REST get operation, the
      field mask applies directly to the response, but in case of a REST
      list operation, the mask instead applies to each individual message
      in the returned resource list. In case of a REST custom method,
      other definitions may be used. Where the mask applies will be
      clearly documented together with its declaration in the API.  In
      any case, the effect on the returned resource/resources is required
      behavior for APIs.

      # Field Masks in Update Operations

      A field mask in update operations specifies which fields of the
      targeted resource are going to be updated. The API is required
      to only change the values of the fields as specified in the mask
      and leave the others untouched. If a resource is passed in to
      describe the updated values, the API ignores the values of all
      fields not covered by the mask.

      If a repeated field is specified for an update operation, the existing
      repeated values in the target resource will be overwritten by the new values.
      Note that a repeated field is only allowed in the last position of a `paths`
      string.

      If a sub-message is specified in the last position of the field mask for an
      update operation, then the existing sub-message in the target resource is
      overwritten. Given the target message:

          f {
            b {
              d : 1
              x : 2
            }
            c : 1
          }

      And an update message:

          f {
            b {
              d : 10
            }
          }

      then if the field mask is:

       paths: "f.b"

      then the result will be:

          f {
            b {
              d : 10
            }
            c : 1
          }

      However, if the update mask was:

       paths: "f.b.d"

      then the result would be:

          f {
            b {
              d : 10
              x : 2
            }
            c : 1
          }

      In order to reset a field's value to the default, the field must
      be in the mask and set to the default value in the provided resource.
      Hence, in order to reset all fields of a resource, provide a default
      instance of the resource and set all fields in the mask, or do
      not provide a mask as described below.

      If a field mask is not present on update, the operation applies to
      all fields (as if a field mask of all fields has been specified).
      Note that in the presence of schema evolution, this may mean that
      fields the client does not know and has therefore not filled into
      the request will be reset to their default. If this is unwanted
      behavior, a specific service may require a client to always specify
      a field mask, producing an error if not.

      As with get operations, the location of the resource which
      describes the updated values in the request message depends on the
      operation kind. In any case, the effect of the field mask is
      required to be honored by the API.

      ## Considerations for HTTP REST

      The HTTP kind of an update operation which uses a field mask must
      be set to PATCH instead of PUT in order to satisfy HTTP semantics
      (PUT must only be used for full updates).

      # JSON Encoding of Field Masks

      In JSON, a field mask is encoded as a single string where paths are
      separated by a comma. Fields name in each path are converted
      to/from lower-camel naming conventions.

      As an example, consider the following message declarations:

          message Profile {
            User user = 1;
            Photo photo = 2;
          }
          message User {
            string display_name = 1;
            string address = 2;
          }

      In proto a field mask for `Profile` may look as such:

          mask {
            paths: "user.display_name"
            paths: "photo"
          }

      In JSON, the same mask is represented as below:

          {
            mask: "user.displayName,photo"
          }

      # Field Masks and Oneof Fields

      Field masks treat fields in oneofs just as regular fields. Consider the
      following message:

          message SampleMessage {
            oneof test_oneof {
              string name = 4;
              SubMessage sub_message = 9;
            }
          }

      The field mask can be:

          mask {
            paths: "name"
          }

      Or:

          mask {
            paths: "sub_message"
          }

      Note that oneof type names ("test_oneof" in this case) cannot be used in
      paths.
    title: '`FieldMask` represents a set of symbolic field paths, for example:'
  vendastatypesPagedRequestOptions:
    type: object
    properties:
      cursor:
        type: string
        title: cursor can be passed to retrieve the next page of results keyed by the cursor
      pageSize:
        type: string
        format: int64
        title: page_size specifies the number of items to return in the next page
  vendastatypesPagedResponseMetadata:
    type: object
    properties:
      nextCursor:
        type: string
        title: A cursor that can be provided to retrieve the next page of results
      hasMore:
        type: boolean
        title: Whether or not more results exist
      totalResults:
        type: string
        format: int64
        title: The total number of results. This is not supported on all paged apis and will be 0 if it is not supported
