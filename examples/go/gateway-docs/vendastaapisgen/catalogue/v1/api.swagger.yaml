swagger: "2.0"
info:
  title: catalogue/v1/api.proto
  version: version not set
tags:
  - name: Ser
consumes:
  - application/json
produces:
  - application/json
paths:
  /catalogue.v1.Ser/List:
    post:
      operationId: Ser_List
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/v1GetResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            type: object
            properties: {}
      tags:
        - Ser
definitions:
  protobufAny:
    type: object
    properties:
      '@type':
        type: string
    additionalProperties: {}
  rpcStatus:
    type: object
    properties:
      code:
        type: integer
        format: int32
      message:
        type: string
      details:
        type: array
        items:
          type: object
          $ref: '#/definitions/protobufAny'
  v1GetResponse:
    type: object
    properties:
      servers:
        type: array
        items:
          type: object
          $ref: '#/definitions/v1Server'
  v1Method:
    type: object
    properties:
      name:
        type: string
      isClientStream:
        type: boolean
      isServerStream:
        type: boolean
  v1Server:
    type: object
    properties:
      serverName:
        type: string
      methods:
        type: array
        items:
          type: object
          $ref: '#/definitions/v1Method'
