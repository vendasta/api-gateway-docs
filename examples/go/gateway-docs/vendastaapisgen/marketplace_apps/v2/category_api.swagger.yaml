swagger: "2.0"
info:
  title: marketplace_apps/v2/category_api.proto
  version: version not set
tags:
  - name: Category
consumes:
  - application/json
produces:
  - application/json
paths:
  /marketplaceapps.v2.Category/DeleteCategory:
    post:
      operationId: Category_DeleteCategory
      responses:
        "200":
          description: A successful response.
          schema:
            type: object
            properties: {}
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/v2DeleteCategoryRequest'
      tags:
        - Category
  /marketplaceapps.v2.Category/GetCategories:
    post:
      operationId: Category_GetCategories
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/v2GetCategoriesResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/v2GetCategoriesRequest'
      tags:
        - Category
  /marketplaceapps.v2.Category/Upsert:
    post:
      summary: Upsert updates or inserts a record depending on whether id is provided or not.
      operationId: Category_Upsert
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/v2UpsertResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/v2UpsertRequest'
      tags:
        - Category
definitions:
  protobufAny:
    type: object
    properties:
      '@type':
        type: string
    additionalProperties: {}
  rpcStatus:
    type: object
    properties:
      code:
        type: integer
        format: int32
      message:
        type: string
      details:
        type: array
        items:
          type: object
          $ref: '#/definitions/protobufAny'
  v2DeleteCategoryRequest:
    type: object
    properties:
      categoryId:
        type: string
  v2GetCategoriesRequest:
    type: object
  v2GetCategoriesResponse:
    type: object
    properties:
      category:
        type: array
        items:
          type: object
          $ref: '#/definitions/v2MarketplaceCategory'
  v2MarketplaceCategory:
    type: object
    properties:
      id:
        type: string
      name:
        type: string
      subCategories:
        type: array
        items:
          type: object
          $ref: '#/definitions/v2MarketplaceCategory'
  v2UpsertRequest:
    type: object
    properties:
      categoryId:
        type: string
      categoryName:
        type: string
      parentId:
        type: string
  v2UpsertResponse:
    type: object
    properties:
      categoryId:
        type: string
