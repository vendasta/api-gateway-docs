{
  "private": true,
  "scripts": {
    "start": "cd docusaurus && npm run gen-all && docusaurus start",
    "build": "cd docusaurus && npm run gen-all && docusaurus build",
    "serve": "cd docusaurus && docusaurus serve",
    "gen-all": "cd docusaurus && docusaurus gen-api-docs all",
    "clean-all": "cd docusaurus && docusaurus clean-api-docs all"
  }
}
